using UnityEngine;

public class BoxPropertyManager : MonoBehaviour
{

    public string boxName;
    public int boxSize;
    public int boxAge;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
